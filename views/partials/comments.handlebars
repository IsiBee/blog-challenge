<div class="comment">
    {{#each this}}
    <section class="card">
        <div class="card-header comment">
            {{user.username}} on {{format_date created_at}}

        </div>
        <div class="p-1">{{comment_text}}</div>

    </section>
    {{/each}}
</div>